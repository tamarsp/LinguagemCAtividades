// Online C compiler to run C program online
#include <stdio.h>

int main() {
    float peso, valorPagar;
    float valorPrato = 12.00;
    printf("Informe o peso do prato: ");
    scanf("%f", &peso);
    
    valorPagar = peso * valorPrato;
    printf("O total é %.2f\n", valorPagar);
    
    return 0;
}
