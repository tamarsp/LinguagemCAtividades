#include <stdio.h>
int main() {
    // Write C code here
    int f, c;
    printf("Digite a temperatura em fahrenheits: "); scanf("%d", &f);
    c = (f-32)*(5/9);
    printf("resultado é \n %d", c);
    return 0;
}
