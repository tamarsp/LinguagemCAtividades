// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>  
int main() {
    float sm, salarioFunc, quanti;
    printf("Informe o valor do salario Minimo ");
    scanf("%f", &sm);
      printf("Informe o salario do funcionario ");
    scanf("%f", &salarioFunc);
    
    quanti = sm / salarioFunc;
    printf("Quantidade de %.2f", quanti);
    
    return 0;
}
