// Online C compiler to run C program online
#include <stdio.h>
    
    int main(){
   int a;
   printf("Informe um numero: "); scanf("%d", &a);
   
   if(a % 2 == 0)
   printf("Verdadeiro");
   else
   printf("Falso");
  
    return 0;
}
